[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Persist scan/image uploads across redeploys:
# 1) Railway → service → Volumes → add volume, mount path: /data/uploads
# 2) Variables → UPLOAD_DIR=/data/uploads
